<%= form_for([@staff_position, @staff_candidate]) do |f| %>
  <% if @staff_candidate.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@staff_candidate.errors.count, "error") %> prohibited this staff_candidate from being saved:</h2>

      <ul>
      <% @staff_candidate.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :key_result1 %><br />
    <%= f.text_field :key_result1 %>
  </div>

  <div class="field">
    <%= f.label :key_result2 %><br />
    <%= f.text_field :key_result2 %>
  </div>

  <div class="field">
    <%= f.label :key_result3 %><br />
    <%= f.text_field :key_result3 %>
  </div>

  <div class="actions">
    <%= f.submit 'Crear candidatura' %>
  </div>
<% end %>
