<% content_support do %>

<% end -%>

<% content_main do 
params[:page] = 1 if params[:page].nil?
%>  
<%= mfcontents_summaries('Noticias', 
						Term.single_toplevel(:slug => 'gm'),
						[:published, {:content_type => 'News', :order => 'created_on desc'}],
						{:cache => "/common/novedades/page_#{params['page']}",
						 :pager => ActionController::Pagination::Paginator.new(self, Term.single_toplevel(:slug => 'gm').news.count(:published, :content_type => 'News'), 15, params[:page]),
						:show_day_separator => true})%>
<% end -%>
