<%=mfcontents_basic(
    "Más populares en #{forum.root.code.upcase}",
    Proc.new { forum.most_active_items('Topic') },
    [],
    :faction_favicon => true,
    :cache => "/common/foros/_most_active_topics/#{forum.id}/_#{Time.now.to_i/86400}")%>
